const db = require("../data/dbConfig.js");

// to display the users saved notes to them i need a way to get all the notes that have been created by the user
const getUserNotes = id => {

}

// notes saved by the user needs to be added to the database
const addUserNote = (noteObject) => {

}